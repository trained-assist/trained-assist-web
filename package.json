{
  "name": "trained-assist-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/worker-projects.mjs && node test/web-ux.mjs",
    "test:staging": "npm test"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
